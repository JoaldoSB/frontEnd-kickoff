{
  "name": "frontend-kickoff-aavfin",
  "source": "src/index.html",
  "scripts": {
    "start": "parcel",
    "build": "parcel build",
    "api": "json-server --watch db.json",
    "serve": "run-p start api",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "parcel": "^2.7.0"
  }
}
